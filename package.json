{
  "name": "siann",
  "version": "0.1.0",
  "private": true,
  "description": "Developer integration layer for SIANN command bridge workflows.",
  "bin": {
    "siann": "bin/siann.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
